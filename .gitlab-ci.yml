image: golang:1.12

stages:
  - build
  - test

before_script:
  - cp -r /builds/rfaulhaber /go/src/github.com/rfaulhaber
  - cd /go/src/github.com/rfaulhaber/shake
  - go get -u -v

build-project:
  stage: build
  script:
    - go build -v

test-project:
  stage: test
  script:
    - go test -v -cover ./...
